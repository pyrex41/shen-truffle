#!/bin/sh
set -eu
APP=$(CDPATH= cd -- "$(dirname -- "$0")/.." && pwd)
JAVA=${JAVACMD:-${JAVA_HOME:+$JAVA_HOME/bin/}java}
exec "$JAVA" -Xss16m --enable-native-access=ALL-UNNAMED --sun-misc-unsafe-memory-access=allow ${JAVA_OPTS:-} -jar "$APP/lib/shen-truffle.jar" "$@"
